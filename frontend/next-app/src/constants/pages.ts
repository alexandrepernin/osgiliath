export enum Pages {
  SIGNIN = '/sign-in',
  SIGNUP = '/sign-up',
  EMPLOYEES = '/employees',
  LEAVES = '/leaves',
  DASHBOARD = '/',
}

export enum ApiRoutes {
  CREATE_EMPLOYEE = '/api/create-employee',
}
