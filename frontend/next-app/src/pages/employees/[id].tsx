import { SidebarWithHeader } from 'components/Sidebar';

const Page = (): JSX.Element => {
  return (
    <SidebarWithHeader menuIndex={1}>Employee detail page</SidebarWithHeader>
  );
};

export default Page;
